# Production runtime requirements: none beyond Python >= 3.10.
# The simulator, dashboard, edge node, HIL proof, and benchmark are stdlib-only.
# Development tests and offline asset-baking dependencies live in requirements-dev.txt.
